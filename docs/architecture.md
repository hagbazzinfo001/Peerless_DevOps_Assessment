# Architecture

```
React Frontend
        │
        ▼
Express Backend
        │
        ▼
JSON Response
```

The frontend communicates with the backend through REST APIs.

Docker Compose orchestrates both services.

GitHub Actions automates build and testing.